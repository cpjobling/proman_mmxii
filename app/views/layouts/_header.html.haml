%nav#user
  %ul.hmenu
    = render 'devise/menu/registration_items'
    = render 'devise/menu/login_items'
%h1= proman
%h2= yield(:title)
%nav#tabs
  %p
    Tabs: Home, Projects, Contact, About, Help
    -#= navigation [:home, :projects, :contact, :about, :help]
    -#= yield(:subnav)