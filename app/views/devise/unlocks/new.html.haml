- title 'Resend unlock instructions'

- slogan(quotation_generator("Education is the key to unlock the golden door of freedom.", :author => "George Washington Carver"), :born => 1864, :died => 1943)


= semantic_form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| 
  = devise_error_messages!

  = f.inputs do
    =f.input :email

  = f.buttons do
    = f.commit_button "Resend unlock instructions" 

= render :partial => "devise/shared/links"